[project]
name = "ai-api-testing"
version = "0.1.0"
description = "🐦AI test generator for APIs and agentic workflows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "pre-commit>=4.0.1",
    "ruff>=0.8.4",
]
